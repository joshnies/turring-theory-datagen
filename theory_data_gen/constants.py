# Token used for masking unknown source tokens.
MASK_TOKEN = '%mask_n%'

# Token used for prefixing member variables and methods for classes and interfaces.
MEMBER_TOKEN = '%member%'

# Token used for generating a new name for the translated sequence.
DYNAMIC_NAME_TOKEN = '%dyn_name%'

# Common arithmetic operators.
ARITHMETIC_OPS = ['+', '-', '*', '/', '^', '%']

# Common boolean operators.
BOOL_OPS = ['==', '!=', '>', '>=', '<', '<=', '&&', '||']
